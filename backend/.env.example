PORT=8080
DATABASE_URL=postgres://corebank:corebank@localhost:5432/corebank?sslmode=disable
# Generá el tuyo con: openssl rand -hex 32  (o dejalo vacío en dev: se
# autogenera uno al arrancar, pero las sesiones se invalidan en cada reinicio)
JWT_SECRET=
